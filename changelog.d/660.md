- Client-facing Markdown, CSV, XLSX, DOCX, and task reports now use reader-facing finding titles and saved reproduction evidence instead of registry identifiers or collector names. Historic records without sufficient evidence state that limitation explicitly while JSON audit artifacts remain unchanged.
