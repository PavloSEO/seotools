- Reconcile the SQLite artifact documentation with the shipped native body,
  history and offline-reanalysis contracts (#666). The documented late-page
  field table is now checked against the importer mapping, so an older JSONL
  observation remains SQL NULL instead of becoming a fabricated clean value.
