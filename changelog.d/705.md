- Support native SQLite writers, history, retry, snapshots, reanalysis, and project publication on Windows with OS-backed byte-range locks and portable free-space checks. Preserve POSIX directory flushing; Windows file flushing and atomic publication remain available without a POSIX directory-fsync guarantee. Exercise portable workflows in Windows CI and an isolated Linux runtime.
