- URL-mode `crawl-site` now writes a collision-safe native SQLite scan under the caller's `scans/` directory by default. `--out-dir` remains the explicit legacy directory route, while list input and cache live/replay configurations name that route rather than silently changing collection mode.
