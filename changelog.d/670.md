- `render-check --viewport mobile` now uses one stable smartphone diagnostic identity for its raw and browser requests. An explicit `--user-agent` is available through CLI and MCP, and every completed or partial comparison records the effective identity and viewport size.
